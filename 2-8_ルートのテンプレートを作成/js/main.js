// const app = Vue.createApp({
//   // option
// })

// app.mount('#app')


const app = Vue.createApp({
    // option
  }).mount('#app')


  // 引数 ハッシュ マーク アップ を 指定 し て VIEW インスタンス の マウント メソッド を 呼び出し て い ます 。 これ で HTML ページ 内 で View.js
  // の 世界 が 適用 さ れる の は 。 ID イコール アップ を 指定 し た 要素 の 中 です よ という 指定 に なり ます 。 この マウント メソッド を 呼び出す こと により 、
  // 指定 さ れ た HTML 要素 が View.js が 構築 し た DOM に 置き換え られ ます 。 この 動作 の こと を マウント と 呼び ます
